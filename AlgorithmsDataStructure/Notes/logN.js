/*
    Log N base 2

    you have an array A(1) to A(n)
    you divide the array in half recursively
    like in binary search
    wherein you don't need the other half
    that's Log N

    ie: 16 becomes 8 becomes 4 becomes 2 becomes 1
 */